# `features/chapter-hub/`

**章节写作域逻辑**：正文里角色/势力名高亮（`entityPreview`）、光标几何与滚动（`caretGeometry`）、名称补全解析（`nameSuggestUtils`）、共享类型（`types.ts`）。`composables/` 封装与 Vue 生命周期相关的状态与行为，供 `NovelChapterHubView` 组合使用。
